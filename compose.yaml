services:
  scholarius:
    build:
      context: .
      dockerfile: Dockerfile
    image: scholarius:latest
    container_name: scholarius
    ports:
      - "8000:8000"
    restart: unless-stopped
    
